# Python dependencies for the MCP bridge server
mcp>=1.0.0
httpx>=0.27.0
uvicorn>=0.30.0
fastapi>=0.115.0
pydantic>=2.9.0
sockio>=0.15.0
python-socketio>=5.0.0

# Wandelbots Nova SDK — provides `wandelscript` (alias `ws`) CLI globally
wandelbots-nova>=5.0.0
