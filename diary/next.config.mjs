/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "photos.googleapis.com",
      "drive.google.com",
    ],
  },
};

export default nextConfig;
