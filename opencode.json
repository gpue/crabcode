{
  "$schema": "https://opencode.ai/config.json",
  "permission": "allow",
  "provider": {
    "copilot": {
      "id": "copilot",
      "name": "GitHub Copilot"
    }
  },
  "mcp": {
    "linear": {
      "type": "local",
      "command": ["sh", "-c", "echo MCP_LINEAR_START >&2 && exec /usr/bin/linear-mcp-server"],
      "enabled": true,
      "timeout": 30000,
      "environment": {
        "LINEAR_API_KEY": "${LINEAR_API_KEY}"
      }
    }
  }
}
