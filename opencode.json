{
  "$schema": "https://opencode.ai/config.json",
  "permission": "allow",
  "provider": {
    "copilot": {
      "id": "copilot",
      "name": "GitHub Copilot"
    }
  },
  "mcp": {
    "linear": {
      "type": "local",
      "command": ["linear-mcp-server"],
      "enabled": true,
      "timeout": 15000,
      "environment": {
        "LINEAR_API_KEY": "${LINEAR_API_KEY}"
      }
    }
  }
}
